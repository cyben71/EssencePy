# ##########################
# Config file "properties" #
# ##########################

# standard vars
app_name = "My Application "
version = '1.2.3'
debug = true
timeout = 30
valeur_string = toto

# Environment variables (from OS) or dotenv file (.env in conf folder)
var_env=${PWDTEST}
login=${USERNAME}

# hierachical placeholder
placeholder = "My Application_${PWDTEST}"

